/**
 * =============================================================================
 * 迷你桥牌 (Mini-Bridge) - 统一导出入口
 * =============================================================================
 * 
 * 【文件作用】
 * 这个文件是整个 src 目录的"总出口"，集中导出所有类型和工具函数。
 * 其他文件只需要从这个入口导入，代码会更简洁。
 * 
 * 【使用方式】
 * import { Card, Suit, isValidMove, getSortedHand } from './src';
 * 
 * 不需要关心具体来自 types/bridge 还是 utils/BridgeRuleValidator
 * =============================================================================
 */

// 导出所有类型定义
export * from './types/bridge';

// 导出所有规则验证工具
export * from './utils/BridgeRuleValidator';
